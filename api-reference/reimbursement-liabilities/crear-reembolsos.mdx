---
title: "Crear"
---

Crea un nuevo reembolso por pagar en la empresa.

```http
POST /reimbursement-liabilities
```

<Note>
  Este endpoint requiere el scope **`ReimbursementLiabilities.Write`**.
</Note>

### Body parameters

<ParamField body="number" type="string">
  Número del reembolso. Máximo 30 caracteres.

  - Si la [configuración de CxP](/api-reference/account-payable-config/listar-config) tiene el campo `duplicatedNumbers` como `Deny`, y el número ya existe, la solicitud será rechazada con un error `400 Bad Request`.
</ParamField>

<ParamField body="date" type="date" required>
  Fecha del reembolso en formato `YYYY-MM-DD`.
  
  - Debe ser menor o igual a la fecha de hoy.
  - Debe pertenecer a un período fiscal activo.
</ParamField>

<ParamField body="vendorId" type="integer" required>
  Identificador del proveedor (Vendor).

  - Debe ser un asociado con el campo `IsVendor` como true.
</ParamField>

<ParamField body="identityDocumentId" type="integer">
  Identificador del tipo de documento de identidad.
</ParamField>

<ParamField body="identityDocumentNumber" type="string">
  Número de documento de identidad. Máximo 24 caracteres.
</ParamField>

<ParamField body="operationCategory" type="string">
  Categoría de la operación logística asociada. Requerido si `operationId` está presente. Valores aceptados:

  - `Shipment` (Embarque)
  - `CustomManagement` (Aduanas)
  - `Additional` (Misceláneos)
  - `InlandFreight` (Transporte Terrestre)
</ParamField>

<ParamField body="operationId" type="integer">
  Identificador de la operación logística asociada. Requerido si `operationCategory` está presente.
</ParamField>

<ParamField body="masterId" type="integer">
  Identificador del registro maestro (Master).
</ParamField>

<ParamField body="paymentTermId" type="integer" required>
  Identificador del término de pago.
</ParamField>

<ParamField body="reference" type="string">
  Referencia del reembolso. Máximo 250 caracteres.
</ParamField>

<ParamField body="description" type="string">
  Descripción general del reembolso. Máximo 4000 caracteres.
</ParamField>

<ParamField body="currencyId" type="string" required>
  Identificador de la moneda.
</ParamField>

<ParamField body="exchangeRate" type="decimal">
  Tasa de cambio aplicada. Debe ser mayor que cero.

  Si el campo es `null`:
  - Si `currencyId` es distinto a la moneda de la empresa, el sistema asignará automáticamente la tasa de cambio vigente.
  - Si `currencyId` es igual a la moneda de la empresa, se asignará por defecto el valor `1`.
</ParamField>

<ParamField body="details" type="array">
  Lista de detalles del reembolso.
</ParamField>

<ParamField body="accounts" type="array">
  Lista de distribución contable adicional del reembolso.
</ParamField>

### Example request

<CodeGroup>

```bash curl
curl --request POST \
  --url "https://api.primenetcloud.com/v1/reimbursement-liabilities" \
  --header "X-Api-Key: YOUR_API_KEY" \
  --header "X-Tenant-Id: TENANT_ID" \
  --header "Content-Type: application/json" \
  --data '{
    "number": "REI-00123",
    "date": "2026-07-13",
    "vendorId": 142,
    "paymentTermId": 2,
    "currencyId": "USD",
    "exchangeRate": 58.50,
    "operationId": 5321,
    "operationCategory": "Shipment",
    "details": [],
    "accounts": []
  }'
```

```javascript JavaScript
const response = await fetch("https://api.primenetcloud.com/v1/reimbursement-liabilities", {
  method: "POST",
  headers: {
    "X-Api-Key": "YOUR_API_KEY",
    "X-Tenant-Id": "TENANT_ID",
    "Content-Type": "application/json",
  },
  body: JSON.stringify({
    number: "REI-00123",
    date: "2026-07-13",
    vendorId: 142,
    paymentTermId: 2,
    currencyId: "USD",
    exchangeRate: 58.50,
    operationId: 5321,
    operationCategory: "Shipment",
    details: [],
    accounts: []
  }),
});

const data = await response.json();
console.log(data);
```

```python Python
import requests

response = requests.post(
    "https://api.primenetcloud.com/v1/reimbursement-liabilities",
    json={
        "number": "REI-00123",
        "date": "2026-07-13",
        "vendorId": 142,
        "paymentTermId": 2,
        "currencyId": "USD",
        "exchangeRate": 58.50,
        "operationId": 5321,
        "operationCategory": "Shipment",
        "details": [],
        "accounts": []
    },
    headers={
        "X-Api-Key": "YOUR_API_KEY",
        "X-Tenant-Id": "TENANT_ID",
    },
)

print(response.json())
```

</CodeGroup>

### Example response

```json
{
  "id": 1054
}
```

### Response fields

<ResponseField name="id" type="integer">
  Identificador único del reembolso creado.
</ResponseField>

### Errores

<ResponseField name="403 Forbidden">
  El API Key no tiene el scope `ReimbursementLiabilities.Write` requerido.
</ResponseField>

<RequestExample>

```json
{
  "number": "REI-00123",
  "date": "2026-07-13",
  "vendorId": 142,
  "identityDocumentId": 1,
  "identityDocumentNumber": "123456789",
  "operationCategory": "Shipment",
  "operationId": 5321,
  "masterId": 890,
  "paymentTermId": 2,
  "reference": "Ref-987",
  "description": "Reembolso por gastos de flete marítimo",
  "currencyId": "USD",
  "exchangeRate": 58.50,
  "details": [],
  "accounts": []
}
```

</RequestExample>