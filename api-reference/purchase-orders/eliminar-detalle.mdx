---
title: "Eliminar Detalle"
api: "DELETE /purchase-orders/{id}/details/{detailId}"
---

Elimina un detalle específico asociado a una orden de compra (purchase order).

<Note>
  Este endpoint requiere el scope **`PurchaseOrders.Write`**.
</Note>

### Headers

<ParamField header="X-Tenant-Id" type="string" required>
  Identificador del tenant al cual pertenece el API Key.
</ParamField>

### Path parameters

<ParamField path="id" type="integer" required>
  Identificador único de la orden de compra.
</ParamField>

<ParamField path="detailId" type="integer" required>
  Identificador único del detalle a eliminar.
</ParamField>

### Example request

<CodeGroup>

```bash curl
curl --request DELETE \
  --url "https://api.primenetcloud.com/v1/purchase-orders/776/details/1" \
  --header "X-Api-Key: YOUR_API_KEY" \
  --header "X-Tenant-Id: TENANT_ID"
```

```javascript JavaScript
const response = await fetch("https://api.primenetcloud.com/v1/purchase-orders/776/details/1", {
  method: "DELETE",
  headers: {
    "X-Api-Key": "YOUR_API_KEY",
    "X-Tenant-Id": "TENANT_ID",
  },
});

console.log(response.status); // 204
```

```python Python
import requests

response = requests.delete(
    "https://api.primenetcloud.com/v1/purchase-orders/776/details/1",
    headers={
        "X-Api-Key": "YOUR_API_KEY",
        "X-Tenant-Id": "TENANT_ID",
    },
)

print(response.status_code)
```

</CodeGroup>

### Response

Si la eliminación es exitosa, el endpoint retorna `204 No Content` sin body.

### Errores

<ResponseField name="403 Forbidden">
  El API Key no tiene el scope `PurchaseOrders.Write` requerido.
</ResponseField>

<ResponseField name="404 Not Found">
  No existe ninguna orden de compra con el `id` especificado, o el `detailId` no pertenece a la orden.
</ResponseField>
