---
title: "Actualizar Parcial Detalle"
api: "PATCH /purchase-orders/{id}/details/{detailId}"
---

Actualiza un detalle parcialmente para una orden de compra específica.

<Note>
  Este endpoint requiere el scope **`PurchaseOrders.Write`**.
</Note>

### Headers

<ParamField header="X-Tenant-Id" type="string" required>
  Identificador del tenant al cual pertenece el API Key.
</ParamField>

### Path parameters

<ParamField path="id" type="integer" required>
  Identificador único de la orden de compra a la que pertenece el detalle.
</ParamField>

<ParamField path="detailId" type="integer" required>
  Identificador único del detalle a actualizar.
</ParamField>

### Body parameters

<ParamField body="itemType" type="string">
  Tipo de ítem a registrar. Valores aceptados:

  - `Charge` (Cargo)
  - `FixedAsset` (Activo Fijo)
  - `Product` (Producto)
</ParamField>

<ParamField body="itemId" type="integer">
  Identificador del ítem asociado.

  - Si `itemType` es `Charge`, debe ser un valor numérico válido del tipo `short`.
</ParamField>

<ParamField body="chargeTypeId" type="integer">
  Identificador del tipo de cargo.

  - Este campo debe ser `null` si el campo `itemType` no es `Charge`.
</ParamField>

<ParamField body="isTransparent" type="boolean" default="false">
  Indica si el cargo es transparente.
</ParamField>

<ParamField body="taxPercent" type="decimal">
  Porcentaje de impuesto a aplicar. Debe ser mayor o igual a cero.
</ParamField>

<ParamField body="description" type="string">
  Descripción del detalle. Máximo 250 caracteres.
</ParamField>

<ParamField body="calculationTypeId" type="string">
  Identificador del tipo de cálculo.
</ParamField>

<ParamField body="quantity" type="decimal">
  Cantidad del ítem. Debe ser mayor que cero.
</ParamField>

<ParamField body="productPackagingId" type="integer">
  Identificador del empaque del producto.
</ParamField>

<ParamField body="packagingQuantity" type="decimal" default="1">
  Cantidad de empaques. Debe ser mayor que cero.
</ParamField>

<ParamField body="price" type="decimal">
  Precio unitario. Debe ser mayor o igual a cero.
</ParamField>

<ParamField body="discount" type="decimal">
  Monto de descuento aplicado. Debe ser mayor o igual a cero.
</ParamField>

<ParamField body="goodServiceApplicability" type="string">
  Aplicabilidad de bienes y servicios. Valores aceptados:

  - `NotApplicable`
  - `Product`
  - `Service`
</ParamField>

<ParamField body="operationCategory" type="string">
  Categoría de la operación logística asociada. Requerido si `operationId` está presente. Valores aceptados:

  - `Shipment` (Embarque)
  - `CustomManagement` (Aduanas)
  - `Additional` (Misceláneos)
  - `InlandFreight` (Transporte Terrestre)
</ParamField>

<ParamField body="operationId" type="integer">
  Identificador de la operación logística asociada. Requerido si `operationCategory` está presente.
</ParamField>

<ParamField body="departmentId" type="integer">
  Identificador del departamento asignado (Dimensión).

  - Debe ser una dimensión con el campo `typeId` igual a `1` (Department).
</ParamField>

<ParamField body="costCenterId" type="integer">
  Identificador del centro de costo asignado (Dimensión).

  - Debe ser una dimensión con el campo `typeId` igual a `2` (CostCenter).
</ParamField>

<ParamField body="projectId" type="integer">
  Identificador del proyecto asignado (Dimensión).

  - Debe ser una dimensión con el campo `typeId` igual a `3` (Project).
</ParamField>

### Example request

<CodeGroup>

```bash curl
curl --request PATCH \
  --url "https://api.primenetcloud.com/v1/purchase-orders/776/details/1" \
  --header "X-Api-Key: YOUR_API_KEY" \
  --header "X-Tenant-Id: TENANT_ID" \
  --header "Content-Type: application/merge-patch+json" \
  --data '{
    "price": 1600,
    "discount": 50
  }'
```

```javascript JavaScript
const response = await fetch("https://api.primenetcloud.com/v1/purchase-orders/776/details/1", {
  method: "PATCH",
  headers: {
    "X-Api-Key": "YOUR_API_KEY",
    "X-Tenant-Id": "TENANT_ID",
    "Content-Type": "application/merge-patch+json",
  },
  body: JSON.stringify({
    price: 1600,
    discount: 50
  }),
});

const data = await response.json();
console.log(data);
```

```python Python
import requests

response = requests.patch(
    "https://api.primenetcloud.com/v1/purchase-orders/776/details/1",
    json={
        "price": 1600,
        "discount": 50
    },
    headers={
        "X-Api-Key": "YOUR_API_KEY",
        "X-Tenant-Id": "TENANT_ID",
        "Content-Type": "application/merge-patch+json"
    },
)

print(response.json())
```

</CodeGroup>

### Response

Si la actualización es exitosa, el endpoint retorna `204 No Content` sin body.

### Errores

<ResponseField name="404 Not Found">
  No existe ninguna orden de compra con el `id` especificado en la URL, o el `detailId` no pertenece a la orden.
</ResponseField>

<ResponseField name="403 Forbidden">
  El API Key no tiene el scope `PurchaseOrders.Write` requerido.
</ResponseField>

<ResponseField name="415 Unsupported Media Type">
  La solicitud no incluye el header `Content-Type` como `application/merge-patch+json`.
</ResponseField>

<RequestExample>

```json
{
  "price": 1600.0000,
  "discount": 50.00
}
```

</RequestExample>
