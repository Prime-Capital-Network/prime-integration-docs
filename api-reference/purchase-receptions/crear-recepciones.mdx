---
title: "Crear"
api: "POST /purchase-receptions"
---

Crea una nueva recepción de compra en la empresa.

<Note>
  Este endpoint requiere el scope **`PurchaseReceptions.Write`**.
</Note>

### Headers

<ParamField header="X-Tenant-Id" type="string" required>
  Identificador del tenant al cual pertenece el API Key.
</ParamField>

### Body parameters

<ParamField body="purchaseOrderId" type="integer">
  Identificador de la orden de compra contra la cual se recibe la mercancía.
</ParamField>

<ParamField body="date" type="date" required>
  Fecha de la recepción de compra en formato `YYYY-MM-DD`.
</ParamField>

<ParamField body="vendorId" type="integer">
  Identificador del proveedor (Vendor).

  - Debe ser un asociado con el campo `IsVendor` como true.
</ParamField>

<ParamField body="observations" type="string">
  Observaciones de la recepción de compra. Máximo 250 caracteres.
</ParamField>

import DetailFields from './snippets/purchase-reception-detail-fields.mdx'

<ParamField body="details" type="array">
  Lista de detalles de la recepción de compra.
  <Expandable title="detalle">
    <DetailFields />
  </Expandable>
</ParamField>

### Example request

<CodeGroup>

```bash curl
curl --request POST \
  --url "https://api.primenetcloud.com/v1/purchase-receptions" \
  --header "X-Api-Key: YOUR_API_KEY" \
  --header "X-Tenant-Id: TENANT_ID" \
  --header "Content-Type: application/json" \
  --data '{
    "purchaseOrderId": 776,
    "date": "2026-07-15",
    "vendorId": 142,
    "observations": "Recepción parcial del contenedor MSCU1234567",
    "details": [
      {
        "productId": 88,
        "quantity": 800,
        "purchaseOrderDetailId": 34,
        "productPackagingId": 10,
        "packagingQuantity": 20
      }
    ]
  }'
```

```javascript JavaScript
const response = await fetch("https://api.primenetcloud.com/v1/purchase-receptions", {
  method: "POST",
  headers: {
    "X-Api-Key": "YOUR_API_KEY",
    "X-Tenant-Id": "TENANT_ID",
    "Content-Type": "application/json",
  },
  body: JSON.stringify({
    purchaseOrderId: 776,
    date: "2026-07-15",
    vendorId: 142,
    observations: "Recepción parcial del contenedor MSCU1234567",
    details: [
      {
        productId: 88,
        quantity: 800,
        purchaseOrderDetailId: 34,
        productPackagingId: 10,
        packagingQuantity: 20
      }
    ]
  }),
});

const data = await response.json();
console.log(data);
```

```python Python
import requests

response = requests.post(
    "https://api.primenetcloud.com/v1/purchase-receptions",
    json={
        "purchaseOrderId": 776,
        "date": "2026-07-15",
        "vendorId": 142,
        "observations": "Recepción parcial del contenedor MSCU1234567",
        "details": [
            {
                "productId": 88,
                "quantity": 800,
                "purchaseOrderDetailId": 34,
                "productPackagingId": 10,
                "packagingQuantity": 20
            }
        ]
    },
    headers={
        "X-Api-Key": "YOUR_API_KEY",
        "X-Tenant-Id": "TENANT_ID",
    },
)

print(response.json())
```

</CodeGroup>

### Example response

```json
{
  "id": 512
}
```

### Response fields

<ResponseField name="id" type="integer">
  Identificador único de la recepción de compra creada.
</ResponseField>

### Errores

<ResponseField name="403 Forbidden">
  El API Key no tiene el scope `PurchaseReceptions.Write` requerido.
</ResponseField>

<RequestExample>

```json
{
  "purchaseOrderId": 776,
  "date": "2026-07-15",
  "vendorId": 142,
  "observations": "Recepción parcial del contenedor MSCU1234567",
  "details": [
    {
      "productId": 88,
      "quantity": 800.0000,
      "purchaseOrderDetailId": 34,
      "productPackagingId": 10,
      "packagingQuantity": 20.0000
    }
  ]
}
```

</RequestExample>
