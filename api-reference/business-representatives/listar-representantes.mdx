---
title: "Listar"
sidebarTitle: "Listar"
description: "Permite manejar representantes mediante REST API"
api: "GET /business-representatives"
---

Obtén una lista paginada de representantes en la empresa.

<Note>
  Este endpoint requiere el scope **`BusinessRepresentatives.Read`**.
</Note>

### Headers

<ParamField header="X-Tenant-Id" type="string" required>
  Identificador del tenant al cual pertenece el API Key.
</ParamField>

### Query parameters

<ParamField query="filter_by" type="string">
  Valores aceptados:

  - `Active`
</ParamField>

<ParamField query="name" type="StringSearchFilter">
  Filtrar por el nombre del representante.
</ParamField>

<ParamField query="isSalesPerson" type="BooleanSearchFilter">
  Filtrar por si el representante es un vendedor.
</ParamField>

<ParamField query="isExecutiveAccount" type="BooleanSearchFilter">
  Filtrar por si el representante es un ejecutivo de cuentas.
</ParamField>

<ParamField query="isCustomerService" type="BooleanSearchFilter">
  Filtrar por si el representante pertenece al área de servicio al cliente.
</ParamField>

<ParamField query="isDebtCollector" type="BooleanSearchFilter">
  Filtrar por si el representante es un gestor de cobros o cobrador.
</ParamField>

<ParamField query="isActive" type="BooleanSearchFilter">
  Filtrar por si está activo o no.
</ParamField>

<ParamField query="InsertedBy" type="StringSearchFilter">
  Filtra por el usuario que creó el representante.
</ParamField>

<ParamField query="InsertedDate" type="RangeSearchFilter<datetime>">
  Filtra por la fecha de creación del representante.
</ParamField>

<ParamField query="UpdatedBy" type="StringSearchFilter">
  Filtra por el usuario que actualizó por última vez el representante.
</ParamField>

<ParamField query="UpdatedDate" type="RangeSearchFilter<datetime>">
  Filtra por la fecha de última actualización del representante.
</ParamField>

### Example request

<CodeGroup>

```bash curl
curl --request GET \
  --url "https://api.primenetcloud.com/v1/business-representatives?isActive=true&isSalesPerson=true" \
  --header "X-Api-Key: YOUR_API_KEY" \
  --header "X-Tenant-Id: TENANT_ID"
```

```javascript JavaScript
const response = await fetch(
  "https://api.primenetcloud.com/v1/business-representatives?isActive=true&isSalesPerson=true",
  {
    method: "GET",
    headers: {
      "X-Api-Key": "YOUR_API_KEY",
      "X-Tenant-Id": "TENANT_ID",
    },
  }
);

const data = await response.json();
console.log(data);
```

```python Python
import requests

response = requests.get(
    "https://api.primenetcloud.com/v1/business-representatives",
    params={
        "isActive": "true",
        "isSalesPerson": "true",
    },
    headers={
        "X-Api-Key": "YOUR_API_KEY",
        "X-Tenant-Id": "TENANT_ID",
    },
)

print(response.json())
```

</CodeGroup>

### Example response

```json
[
  {
    "id": 0,
    "name": "string",
    "isSalesPerson": true,
    "isExecutiveAccount": true,
    "isCustomerService": true,
    "isDebtCollector": true,
    "isActive": true,
    "insertedBy": "string",
    "insertedDate": "2026-07-01T10:00:00.000Z",
    "updatedBy": "string",
    "updatedDate": "2026-07-05T14:30:00.000Z"
  }
]
```

### Response fields

<ResponseField name="id" type="integer">
  Identificador único del representante.
</ResponseField>

<ResponseField name="name" type="string">
  Nombre completo del representante.
</ResponseField>

<ResponseField name="isSalesPerson" type="boolean">
  Indica si el representante es un vendedor.
</ResponseField>

<ResponseField name="isExecutiveAccount" type="boolean">
  Indica si el representante es un ejecutivo de cuentas.
</ResponseField>

<ResponseField name="isCustomerService" type="boolean">
  Indica si el representante pertenece al área de servicio al cliente.
</ResponseField>

<ResponseField name="isDebtCollector" type="boolean">
  Indica si el representante es un gestor de cobros o cobrador.
</ResponseField>

<ResponseField name="isActive" type="boolean">
  Indica si el representante está actualmente activo en el sistema.
</ResponseField>

<ResponseField name="insertedBy" type="string">
  Usuario que creó el representante.
</ResponseField>

<ResponseField name="insertedDate" type="datetime">
  Fecha y hora de creación del representante.
</ResponseField>

<ResponseField name="updatedBy" type="string">
  Usuario que actualizó por última vez el representante. `null` si no ha sido actualizado.
</ResponseField>

<ResponseField name="updatedDate" type="datetime">
  Fecha y hora de la última actualización del representante. `null` si no ha sido actualizado.
</ResponseField>