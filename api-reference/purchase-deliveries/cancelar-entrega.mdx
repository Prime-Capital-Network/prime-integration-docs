---
title: "Cancelar"
api: "POST /purchase-deliveries/{id}/cancel"
---

Cancela una entrega de compra en la empresa.

<Note>
  Este endpoint requiere el scope **`PurchaseDeliveries.Write`**.
</Note>

### Headers

<ParamField header="X-Tenant-Id" type="string" required>
  Identificador del tenant al cual pertenece el API Key.
</ParamField>

### Path parameters

<ParamField path="id" type="integer" required>
  Identificador único de la entrega de compra a cancelar.
</ParamField>

### Body parameters

<ParamField body="reason" type="string">
  Razón de la cancelación. Máximo 250 caracteres.
</ParamField>

### Example request

<CodeGroup>

```bash curl
curl --request POST \
  --url "https://api.primenetcloud.com/v1/purchase-deliveries/318/cancel" \
  --header "X-Api-Key: YOUR_API_KEY" \
  --header "X-Tenant-Id: TENANT_ID" \
  --header "Content-Type: application/json" \
  --data '{
    "reason": "Solicitud de compra cancelada por el requester"
  }'
```

```javascript JavaScript
const response = await fetch("https://api.primenetcloud.com/v1/purchase-deliveries/318/cancel", {
  method: "POST",
  headers: {
    "X-Api-Key": "YOUR_API_KEY",
    "X-Tenant-Id": "TENANT_ID",
    "Content-Type": "application/json",
  },
  body: JSON.stringify({
    "reason": "Solicitud de compra cancelada por el requester"
  }),
});

const data = await response.json();
console.log(data);
```

```python Python
import requests

response = requests.post(
    "https://api.primenetcloud.com/v1/purchase-deliveries/318/cancel",
    json={
        "reason": "Solicitud de compra cancelada por el requester"
    },
    headers={
        "X-Api-Key": "YOUR_API_KEY",
        "X-Tenant-Id": "TENANT_ID",
    },
)

print(response.json())
```

</CodeGroup>

### Response

Si la cancelación es exitosa, el endpoint retorna `204 No Content` sin body.

### Errores

<ResponseField name="400 Bad Request">
  La entrega de compra ya ha sido cancelada.
</ResponseField>

<ResponseField name="404 Not Found">
  No existe ninguna entrega de compra con el `id` especificado en la URL.
</ResponseField>

<ResponseField name="403 Forbidden">
  El API Key no tiene el scope `PurchaseDeliveries.Write` requerido.
</ResponseField>

<RequestExample>

```json
{
  "reason": "Solicitud de compra cancelada por el requester"
}
```

</RequestExample>
